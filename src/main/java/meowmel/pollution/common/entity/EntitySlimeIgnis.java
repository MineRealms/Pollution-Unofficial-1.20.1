package meowmel.pollution.common.entity;

import net.minecraft.world.entity.EntityType;
import net.minecraft.world.level.Level;

/** Ignis slime: the Thaumcraft fire-aspected slime variant. */
public class EntitySlimeIgnis extends EntityTcSlime {

    public EntitySlimeIgnis(EntityType<? extends EntitySlimeIgnis> entityType, Level level) {
        super(entityType, level);
    }
}
