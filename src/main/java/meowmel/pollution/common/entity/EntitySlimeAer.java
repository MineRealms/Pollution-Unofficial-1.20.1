package meowmel.pollution.common.entity;

import net.minecraft.world.entity.EntityType;
import net.minecraft.world.level.Level;

/** Aer slime: the Thaumcraft air-aspected slime variant. */
public class EntitySlimeAer extends EntityTcSlime {

    public EntitySlimeAer(EntityType<? extends EntitySlimeAer> entityType, Level level) {
        super(entityType, level);
    }
}
