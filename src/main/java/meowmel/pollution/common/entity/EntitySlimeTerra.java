package meowmel.pollution.common.entity;

import net.minecraft.world.entity.EntityType;
import net.minecraft.world.level.Level;

/** Terra slime: the Thaumcraft earth-aspected slime variant. */
public class EntitySlimeTerra extends EntityTcSlime {

    public EntitySlimeTerra(EntityType<? extends EntitySlimeTerra> entityType, Level level) {
        super(entityType, level);
    }
}
