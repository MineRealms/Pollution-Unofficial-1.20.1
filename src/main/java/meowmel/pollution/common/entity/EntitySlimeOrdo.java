package meowmel.pollution.common.entity;

import net.minecraft.world.entity.EntityType;
import net.minecraft.world.level.Level;

/** Ordo slime: the Thaumcraft order-aspected slime variant. */
public class EntitySlimeOrdo extends EntityTcSlime {

    public EntitySlimeOrdo(EntityType<? extends EntitySlimeOrdo> entityType, Level level) {
        super(entityType, level);
    }
}
